# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
name: jhipstercommerce
services:
  mariadb:
    extends:
      file: ./mariadb.yml
      service: mariadb
    profiles:
      - ''
      - prod
  keycloak:
    extends:
      file: ./keycloak.yml
      service: keycloak
